# Rules documentation index

As requested, rules are split into two documents:

- **Generic implementation details (syntax/conventions):** `src/RULES_GENERIC.md`
- **Program-specific rules (APIs, tables, behavior):** `src/RULES_PROGRAM.md`

Use `RULES_GENERIC.md` when creating/changing code style and implementation patterns.
Use `RULES_PROGRAM.md` when changing business behavior for this OCI Starter demo.